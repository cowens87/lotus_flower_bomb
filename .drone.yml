kind: pipeline
type: docker
name: default

steps:
- name: test
  image: ruby:2.5.3
  commands:
    - bundle install 
    - rake 
    # - bundle exec rake db:create db:schema:load test
services:


# pipeline:
#   build:
#     image: ruby:2.5.3
#     commands:
#       - bundle install 
#       # - rake
#       - bundle exec rails db:create db:schema:load test